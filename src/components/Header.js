import React,{Component} from 'react'

/**
 * Top menu component, included to each page if user authenticated
 */
export default class LoginForm extends Component {

    /**
     * Method used to render component on the screen
     */
    render() {
        return (
            <div className="navbar navbar-default" key="header">
                Header
            </div>
        )
    }
}
